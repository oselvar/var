[package]
name = "varar-runner"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
varar-core = { path = "../varar-core" }
varar-config = { path = "../varar-config" }
regex = "1"

[lib]
name = "varar_runner"
path = "src/lib.rs"
